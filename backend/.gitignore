node_modules/
*.db
*.sqlite
.env

# Пруфы — живые фото и видео пользователей (лица людей). В репозитории им
# не место: репозиторий публичный, а по 152-ФЗ это персональные данные.
# На сервере они лежат на volume (PROOFS_DIR), локально — здесь.
db/proofs/
